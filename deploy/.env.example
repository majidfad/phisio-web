# Deprecated: unified .env lives at /opt/phisio/.env (owned by phisio-api bootstrap).
# Web deploy writes PHISIO_WEB_IMAGE into that file.
# Kept for reference only.

PHISIO_WEB_IMAGE=phisio-web:pending
HTTP_PORT=3000

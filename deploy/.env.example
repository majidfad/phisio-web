# Deprecated local example — production uses /opt/phisio/.env from phisio-api CI.
# Web image is a mirrored GHCR ref, e.g. focker.ir/ghcr.io/<owner>/phisio-web:<git-sha>

PHISIO_WEB_IMAGE=
HTTP_PORT=3000
